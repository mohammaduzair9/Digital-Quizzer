<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quizer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.ScoreActivity" />
        <activity android:name=".controller.StudentMainActivity" />
        <activity android:name=".controller.InstructorMainActivity" />
        <activity android:name=".controller.StudentSubActivity" />
        <activity android:name=".controller.InstructorSubActivity" />
        <activity android:name=".controller.LoginActivity" />

    </application>

</manifest>